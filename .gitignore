/target
/build

# Repo-root convenience symlink to the freshly-built AppImage in build/
/CleanMic.AppImage

# Pre-built third-party binaries (download via scripts/fetch-vendors.sh)
/vendor/

# Compiled locale files (generated by make mo)
*.mo

# Downloaded packages
*.deb

# Workflow / planning artifacts — MUST NEVER be committed (private state, leaked
# previously and forced repo cleanup). Includes the .planning symlink to the
# sibling private repo at ../CleanMic-planning.
/.planning
/.planning/
/.claude/
/CLAUDE.md
/docs/
/rust_out
